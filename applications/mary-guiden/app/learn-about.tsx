import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function LearnAbout() {
  return (
    <SafeAreaView>
      <View>
        <Text>Learn about Page</Text>
      </View>
    </SafeAreaView>
  );
}
